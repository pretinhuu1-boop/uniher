# UniHER Wave B Implementation Ledger

Date: 2026-07-29
Source matrix: `docs/superpowers/audits/2026-07-29-uniher-safe-parity-matrix.md`
Baseline: `9ca4bd8d9d60c25a0b116d8ded0009cff831a551`
Current source: `origin/main`
Next product decision sheet: `docs/superpowers/plans/2026-07-29-uniher-gamification-product-decision-sheet.md`

## Rules

- No rollback from the old baseline.
- No code patch without a matrix row labelled `REGRESSION` or `REBUILD_SAFE`.
- Every production patch needs RED then GREEN evidence.
- No deploy or production claim from this ledger.
- Ranking, league, rewards, clinical Semaforo, NR-1/Yavix, SIPAT, Concierge, Denuncias and DH modules stay gated unless separately approved.
- The current public/online landing is frozen. Do not edit or promote `src/app/layout.tsx`, `src/app/proposta/page.tsx`, `src/app/welcome*.tsx`, `src/components/layout/*`, `src/components/sections/*`, or the deployed landing copy inside Wave B unless the user explicitly opens a separate landing wave.

## Queue

| ID | Matrix row | Decision | Scope | First test | Production files | Gate | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| WB-01 | Collaborator exams | `REBUILD_SAFE` | Add collaborator-self capability gate to personal exams API. | `tests/unit/privacy/collaborator-exams-capability.test.ts` | `src/app/api/collaborator/exams/route.ts` | RED/GREEN, collaborator and dual-role allowed, RH/Admin without capability denied. | Implemented locally |
| WB-02 | Colaboradora home visual campaign card | `REGRESSION` | Restore non-gamified campaign card in home summary. | `tests/unit/platform/collaborator-home-summary.test.tsx` | `src/app/(platform)/colaboradora/page.tsx` | RED/GREEN, visible campaign summary, no points/ranking/XP copy. | Implemented locally |
| WB-03 | RH/Admin reset password delivery | `REBUILD_SAFE` | Replace temporary password JSON delivery. | `tests/unit/privacy/password-reset-delivery.test.ts` | `src/app/api/rh/users/[id]/route.ts`, `src/app/api/admin/users/[id]/route.ts`, `src/app/(platform)/colaboradoras-gestao/page.tsx` | No `temporaryPassword`/`tempPassword` in response; `must_change_password=1`; UI does not claim email delivery without contract. | Implemented locally |
| WB-06 | SVG upload under public uploads | `REBUILD_SAFE` | Reject SVG across the central upload helper used by general/avatar/logo uploads and tighten WebP magic-byte validation. | `tests/unit/privacy/upload-svg-block.test.ts` | `src/lib/upload/index.ts`, `src/app/api/upload/route.ts`, `src/app/api/upload/avatar/route.ts`, `src/app/api/upload/logo/route.ts` | RED/GREEN, SVG rejected before file write, no SVG allowlist in operational upload source, client errors return 400, RIFF non-WebP rejected. | Implemented locally |
| WB-07 | Imported employee identity DSAR linkage | `REBUILD_SAFE` | Export imported identity profiles for accepted accounts even when `user_id` is still null, and link profile on invite acceptance. | `tests/unit/privacy/employee-import-privacy.test.ts`, `tests/unit/invite-leadership-capability.test.ts` | `src/lib/privacy/dsar-export.ts`, `src/app/api/invites/[token]/route.ts` | RED/GREEN, same-company same-email fallback, no hash exposure, no cross-company export, atomic invite acceptance link. | Implemented locally |
| WB-08 | Avatar/logo upload tracking | `REBUILD_SAFE` | Pass authenticated user id to avatar/logo upload helper so quota and `user_uploads` tracking apply. | `tests/unit/privacy/upload-user-tracking.test.ts` | `src/app/api/upload/avatar/route.ts`, `src/app/api/upload/logo/route.ts`, `src/app/(platform)/company-profile/page.tsx` | RED/GREEN, avatar/logo call helper with userId, company logo UI matches accepted formats and no SVG offer. | Implemented locally |
| WB-09 | Public health diagnostics exposure | `REBUILD_SAFE` | Keep `/api/health` as public liveness only and leave operational diagnostics behind authenticated/admin system routes. | `tests/unit/privacy/public-health-liveness-boundary.test.ts` | `src/app/api/health/route.ts` | RED/GREEN, only `status` and `timestamp`, no DB/memory/uptime/counts/queue/version, no DB/write queue access. | Implemented locally |
| WB-10 | Stale E2E health and streak contracts | `REBUILD_SAFE` | Align old E2E release gates with WB-09 liveness-only health and current private wellbeing streak payload. | `tests/e2e/master.spec.ts`, `tests/e2e/integrado.spec.ts`, `tests/e2e/seguranca.spec.ts` | Test gates only. | E2E must reject public health diagnostics, tolerate dev-only admin diagnostics returning 403 in production, and keep streak free of points/ranking/league/badge fields. | Implemented locally |
| WB-11 | LGPD deletion request notification routing | `REBUILD_SAFE` | Notify Master Admins as well as same-company admins when a user requests account deletion. | `tests/unit/privacy/delete-request-routing.test.ts` | `src/app/api/users/me/delete-request/route.ts` | RED/GREEN, Master Admin included, same-company admin included, other-company admin excluded, activity log preserved. | Implemented locally |
| WB-12 | Auth first-access semantic gate | `OK_CURRENT` | Add executable parity coverage for login and `/api/auth/me` first-access projections. | `tests/unit/auth-first-access-projection.test.ts` | Test gate only. | Must-change password stays on first-access password step, pending tour stays on first-access, ready users without explicit preference are treated complete, and safe projection omits legacy/private fields. | Implemented locally |
| WB-13 | Browser auth redirect/onboarding smoke | `OK_CURRENT` | Add real browser-flow login coverage for role redirect, unsafe redirect rejection, and pending first-access routing. | `tests/e2e/auth-redirect.spec.ts` | Test gate only. | Admin ignores redirect and lands `/admin`; RH honors safe redirect after completed first access and rejects unsafe external/protocol-relative redirects; lideranca/colaboradora default correctly; pending tour overrides redirect; existing auth session resolves through cookie-backed `/api/auth/me`. | Implemented locally |
| WB-14 | RH/Lideranca invite department scope smoke | `OK_CURRENT` | Add real login/JWT API E2E coverage for invite listing by tenant/company and leadership department. | `tests/e2e/invite-scope.spec.ts` | Test gate only. | RH sees only own-company invites across departments and excludes a foreign-company sentinel; Lideranca sees exactly same-department invites; Lideranca without department receives empty list. | Implemented locally |
| WB-15 | RH user mutation tenant/dept scope smoke | `OK_CURRENT` | Add real login/JWT API E2E coverage for RH user profile, department, role, block/unblock and cross-tenant mutation boundaries. | `tests/e2e/rh-user-mutation.spec.ts` | Test gate only. | RH can update/block/unblock same-company collaborator; foreign-company department assignment is rejected; foreign-company user mutation is rejected and remains unchanged; mutative spec fails on non-loopback `BASE_URL` unless explicit override is set. | Implemented locally |
| WB-16 | Dashboard tenant/dept scope smoke | `OK_CURRENT` | Add real login/JWT API E2E coverage for Admin Master company scope, RH company override denial, and Lideranca persisted-department scoping. | `tests/e2e/dashboard-scope.spec.ts` | Test gate only. | Admin Master must choose company and sees exact scoped protected exam count; RH cannot override session company; Lideranca uses persisted department over requested filter with exact scoped protected exam count; Lideranca without department is blocked; setup fails on non-loopback `BASE_URL` because it seeds the local Playwright DB directly. | Implemented locally |
| WB-17 | Colaboradora safe journey and company challenges projection | `REBUILD_SAFE` | Add real login/JWT API E2E coverage for the useful collaborator journey while hardening public challenge projection. | `tests/e2e/collaborator-journey.spec.ts` | `src/lib/challenges/api.ts`, `src/app/api/collaborator/challenges/route.ts`, `src/app/api/collaborator/challenges/[id]/route.ts`, `tests/global-teardown.ts`, `tests/playwright.config.ts` | Home/campaign/agenda/exams/check-in/challenges stay personal and company-scoped; leaderboard remains privacy-review; challenges expose only public fields; mutative setup requires loopback and same-DB attestation when external `BASE_URL` is used; teardown cleans WB-17 data. | Implemented locally |
| WB-18 | Dashboard CSV/export safe parity | `OK_CURRENT` | Strengthen executable proof that the dashboard CSV exports only protected public cells and not UI-only/internal view-model fields. | `tests/unit/platform/dashboard-export.test.ts`, `tests/unit/privacy/report-projection.test.ts` | Test gate only. | CSV serializes protected metric union, neutralizes formula cells, keeps suppressed values as privacy messages, and omits action descriptions, department ids/colors, age colors, raw contributor counts and numeric canaries. | Implemented locally |
| WB-19 | Platform visual/product boundary smoke | `REBUILD_SAFE` | Add real browser product-boundary coverage for authenticated HOLD shells and useful non-competitive collaborator surfaces. | `tests/e2e/platform-product-boundary.spec.ts`, `tests/unit/module-shells.test.ts` | `src/components/platform/ContainedSurfacePreview.tsx`, `tests/playwright.config.ts` | Contained module shells remain visibly blocked; Liga/gamification stay in review; Desafios/Conquistas/Objetivos stay non-competitive/private; NR-1 direct runtime is either shell-blocked or production-runtime unavailable; no unsafe CTAs or legacy overclaim copy. | Implemented locally |
| WB-20 | Admin legacy badges and gamification alert reopening | `REBUILD_SAFE` | Remove dead Admin badge CRUD surface and prevent Admin alert send from accepting legacy `challenge`/`gamification` notification types. | `tests/unit/privacy/gamification-safe-projection.test.ts`, `tests/unit/privacy/gamification-api-containment.test.ts` | `src/app/(platform)/admin/page.tsx`, `src/app/api/admin/alerts/send/route.ts` | No Admin `BadgesTab`/`/api/admin/badges` UI path; no Admin alert select option for `challenge`/`gamification`; route rejects legacy types with 400 before DB/write queue; contained badge APIs remain 410. | Implemented locally |
| WB-21 | Gamification product truth and authenticated copy boundary | `REBUILD_SAFE` | Stop active first-access/review-route copy from promising classic gamification, XP, rewards, ranking or leaderboard while implementation remains HOLD. | `tests/unit/privacy/gamification-product-copy-boundary.test.ts` | `src/app/(platform)/primeiro-acesso/page.tsx` | First-access does not promise classic gamification/ranking/rewards/XP/badges; review routes stay explicit about HOLD and cannot promise rewards, redemption, leaderboard or points. Public/online landing is explicitly out of scope/frozen. | Implemented locally; product decision HOLD; landing frozen |
| WB-22 | Public/online landing freeze guardrail | `OK_CURRENT` | Add an executable gate proving Wave B did not touch the current public landing paths. | `tests/unit/privacy/landing-freeze-boundary.test.ts` | Test gate only. | Frozen landing files must match baseline `0fb1d517d9ce91cdab1db3703f33f2611d31abfa`; `git diff --name-only` and `git diff --cached --name-only` must return no frozen landing paths; no landing source, deploy or copy edits are allowed in Wave B. | Implemented locally; landing frozen |
| WB-23 | Recovery artifact P1/P2 consistency | `OK_CURRENT` | Align the full gap map and parity matrix with local P1/P2 repairs so operators do not reopen unsafe/stale reset-password or DSAR tasks. | `tests/unit/privacy/recovery-doc-consistency.test.ts`, `tests/unit/privacy/password-reset-delivery.test.ts` | Docs/test gate only. | Gap map must state WB-03/WB-07 fixed the local P1s; matrix must preserve WB-03 instead of asking for a new failing reset test; reset behavior test remains green. | Implemented locally |
| WB-24 | Production HOLD promotion boundary | `OK_CURRENT` | Add an executable docs/release-preflight canary proving this recovery goal cannot claim deploy/prod readiness without target-host evidence. | `tests/unit/privacy/recovery-production-hold-boundary.test.ts`, `tests/check-release-env-security.test.cjs` | Test gate only. | Current recovery artifacts must say production remains HOLD; `check:release-env` must require release secrets, SQLite blacklist, employee-import HMAC secret and configured smoke accounts before release-like production. | Implemented locally |
| WB-25 | Sensitive module activation boundary | `OK_CURRENT` | Strengthen executable proof that sensitive modules remain HOLD unless a separate contract/source/approval path exists. | `tests/unit/company-modules.test.ts`, `tests/unit/company-modules-api.test.ts`, `tests/unit/module-shells.test.ts`, `tests/unit/privacy/semaforo-containment.test.ts`, `tests/unit/privacy/gamification-api-containment.test.ts` | Test gate only. | Admin module mutation cannot set `primary_health`, `concierge`, `nr1`, `sipat`, `human_development` or `denunciation` to `enabled`; shells remain static/blocked; Semaforo clinical writers and legacy ranking/league/reward APIs remain contained. | Implemented locally; sensitive modules HOLD |
| WB-04 | Individual health/score exposure in dashboard | `REBUILD_SAFE` | Any future health/score parity only through aggregate suppression. | Existing privacy projection tests, plus TBD if changed. | Dashboard/report surfaces only if approved. | No individual health, score, ranking, or reconstruction. | HOLD for now |
| WB-05 | Legacy leaderboard, league and rewards | `HOLD_EXTERNAL` | Product/legal/privacy contract before rebuilding. | TBD only after approval. | Gamification/league/rewards routes. | External approval and safe product spec. | HOLD |

## Current Wave

WB-01, WB-02, WB-03, WB-06, WB-07, WB-08, WB-09, WB-10, WB-11, WB-12, WB-13, WB-14, WB-15, WB-16, WB-17, WB-18, WB-19, WB-20, WB-21, WB-22, WB-23, WB-24 and WB-25 have local receipts. `docs/superpowers/plans/2026-07-29-uniher-gamification-product-decision-sheet.md` remains the product decision source; do not implement gamification/ranking/rewards until that decision is explicit.

## Receipts

| ID | RED evidence | GREEN evidence | Notes |
| --- | --- | --- | --- |
| WB-01 | `npm run test:unit -- tests/unit/privacy/collaborator-exams-capability.test.ts` failed because RH/Admin without collaborator capability received `200` instead of `403`. | `npm run test:unit -- tests/unit/privacy/collaborator-exams-capability.test.ts` passed: 1 file, 4 tests. Combined focused run passed: 2 files, 5 tests. | Production patch imports `hasCollaboratorSelfCapability` and checks it before GET/POST. |
| WB-02 | `npm run test:unit -- tests/unit/platform/collaborator-home-summary.test.tsx` failed because `Campanhas` was missing from `Resumo da jornada`. | `npm run test:unit -- tests/unit/platform/collaborator-home-summary.test.tsx` passed: 1 file, 1 test. Combined focused run passed: 2 files, 5 tests. | Production patch restores only `Campanhas`, `campaignsActive`, and `campaignsTotal`; no points/ranking/XP card. |
| WB-03 | `npm run test:unit -- tests/unit/privacy/password-reset-delivery.test.ts` failed because RH exposed `temporaryPassword`, Admin exposed `tempPassword`, and RH UI still claimed email/password delivery. Claude review then flagged RH `Math.random()` reset-secret generation. | `npm run test:unit -- tests/unit/privacy/password-reset-delivery.test.ts` passed: 1 file, 4 tests. Focused Wave B regression passed: 4 files, 20 tests. | Reset now returns `passwordReset.delivery = 'out_of_band_required'`, sets `must_change_password=1`, removes secret-copy UI, and uses `nanoid` for reset-secret generation. |
| WB-06 | `npm run test:unit -- tests/unit/privacy/upload-svg-block.test.ts` failed because `saveUploadedFile()` accepted `image/svg+xml` and returned `/uploads/general/fixed-upload-id.svg`. Claude review then flagged weak WebP validation accepting generic `RIFF` files. | `npm run test:unit -- tests/unit/privacy/upload-svg-block.test.ts` passed: 1 file, 3 tests. Focused Wave B/P1 regression passed: 6 files, 32 tests. `npx tsc --noEmit` passed. | Central upload helper now allows only JPG, PNG and WebP; general/avatar/logo routes classify helper client errors as 400; WebP validation requires `RIFF....WEBP`. |
| WB-07 | `npm run test:unit -- tests/unit/privacy/employee-import-privacy.test.ts` failed because DSAR returned `employeeIdentityProfile: null` for imported profile with `user_id = NULL`. `npm run test:unit -- tests/unit/invite-leadership-capability.test.ts` failed because invite acceptance did not link the imported profile. | Both focused files passed: employee import privacy 1 file / 3 tests; invite leadership capability 1 file / 2 tests. Focused Wave B/P1 regression passed: 8 files / 38 tests. `npx tsc --noEmit` passed. Claude review found no blocking findings. | DSAR now falls back to same-company same-email unlinked profiles without exporting hashes or cross-company data; invite acceptance links unclaimed same-company same-email profiles in the same write transaction. |
| WB-08 | `npm run test:unit -- tests/unit/privacy/upload-user-tracking.test.ts` failed because avatar/logo routes called `saveUploadedFile()` without the authenticated `userId`; a follow-up RED caught company logo copy saying `PNG, JPG ate 2MB` while accepting WebP/5MB. | `npm run test:unit -- tests/unit/privacy/upload-user-tracking.test.ts` passed: 1 file, 3 tests. Focused Wave B/P1/P2 regression passed: 9 files, 41 tests. `npx tsc --noEmit` passed. Claude review found no blocking findings. | Avatar/logo now pass `context.auth.userId` to the upload helper, so quota and `user_uploads` tracking apply; company logo UI now says `PNG, JPG ou WebP ate 5MB` and does not offer SVG. |
| WB-09 | `npm run test:unit -- tests/unit/privacy/public-health-liveness-boundary.test.ts` failed because `/api/health` exposed `db`, `memory`, `uptime`, `uptimeSeconds`, and `version`. | `npm run test:unit -- tests/unit/privacy/public-health-liveness-boundary.test.ts` passed: 1 file, 1 test. Focused Wave B/P1/P2 regression passed: 10 files, 42 tests. `npx tsc --noEmit` and `git diff --check` passed. Claude review found no blocking findings. | Public health now returns only `status` and `timestamp` behind the existing public rate limit. The boundary test also asserts the route does not touch DB or write queue diagnostics. Existing E2E specs that expected DB/memory on public health are stale and need realignment before they are used as release gates. |
| WB-10 | First run of `cd tests; npx playwright test --config=playwright.config.ts --project=master --project=integrado --project=seguranca` failed because old gates still expected public health diagnostics, `streak-status` only `checkedInToday/streak`, and `/api/admin/system` `200` despite `devOnlyGuard` in production. | Rerun passed: 67 tests / 3 projects. `npx tsc --noEmit` passed. `git diff --check` passed. Stale scan returned `NO_STALE_HEALTH_OR_STREAK_E2E_MATCHES`. Claude review found no blocking findings. | E2E health checks now assert exact public liveness shape and no diagnostic leakage. Integrated streak now accepts the private wellbeing fields while still rejecting points/ranking/league/badges. Master system diagnostics now accept the real production/dev-only 403 gate without weakening public health. |
| WB-11 | `npm run test:unit -- tests/unit/privacy/delete-request-routing.test.ts` failed because only the same-company admin received the LGPD deletion request notification; Master Admin was missing. | Focused test passed: 1 file / 1 test. Focused Wave B/P1/P2 regression passed: 11 files / 43 tests. `npx tsc --noEmit` and `git diff --check` passed. Claude review found no blocking findings. | Delete-request routing now selects same-company admins plus Master Admins (`company_id IS NULL`) and still excludes admins from other companies. The existing `account_deletion_request` activity log receipt is preserved. |
| WB-12 | No runtime RED: matrix row was `OK_CURRENT` but lacked an executable first-access semantic gate. | `npm run test:unit -- tests/unit/auth-first-access-projection.test.ts` passed: 1 file / 3 tests. Focused Wave B regression passed: 12 files / 46 tests. `npx tsc --noEmit` and `git diff --check` passed. Claude review found no blocking findings. | Login and `/api/auth/me` now have a focused parity test for `mustChangePassword`, pending first-access tour, ready-user fallback, and safe user projection without password/gamification fields. |
| WB-13 | Initial browser smoke attempts exposed test fragility: password label strict-mode ambiguity, CNPJ rerun collision, and hydration clearing the email field before submit. Independent review then flagged missing unsafe-redirect negatives and missing `serviceWorkers: 'block'`. | `cd tests; npx playwright test --config=playwright.config.ts --project=auth-redirect` passed: 9 tests / 1 project. `npx tsc --noEmit` and `git diff --check` passed. Claude CLI was installed but blocked by `Not logged in`; independent subagent review findings were addressed. | Added `auth-redirect` Playwright project with real form login, cookie/JWT-backed session checks, safe internal redirect coverage, unsafe external/protocol-relative redirect rejection, pending first-access override, and blocked service workers. No sensitive modules are activated. |
| WB-14 | No runtime RED: matrix row was `OK_CURRENT` but lacked a real login/JWT E2E gate for Lideranca invite listing after the tenant/department hardening rewrite. Independent review then flagged missing foreign-tenant sentinel, loose response assertions, and browser-flow wording for an API-only spec. | `cd tests; npx playwright test --config=playwright.config.ts --project=invite-scope` passed: 3 tests / 1 project after fixes. `npx tsc --noEmit` and `git diff --check` passed. Claude CLI was installed but blocked by `Not logged in`; independent subagent re-review found no remaining blocking findings. | Added `invite-scope` Playwright project with real Admin/RH/Lideranca login, two-company sentinel setup, RH department creation, department assignment, department-specific invites, RH own-company exact listing, Lideranca exact same-department listing, and no-department empty listing. No sensitive modules are activated. |
| WB-15 | No runtime RED: matrix row was `OK_CURRENT` but lacked real login/JWT E2E proof for RH user mutations after tenant/department hardening. Independent review then flagged that the mutative spec could run against external `BASE_URL` without an explicit destructive-test guard. | `cd tests; npx playwright test --config=playwright.config.ts --project=rh-user-mutation` passed: 4 tests / 1 project after adding the loopback guard. `npx tsc --noEmit` and `git diff --check` passed. Claude CLI was installed but blocked by `Not logged in`; independent subagent re-review found no remaining blocking findings. | Added `rh-user-mutation` Playwright project with real Admin/RH login, two-company sentinel setup, department assignment, same-company profile/role update, block/unblock, foreign-department rejection, foreign-user mutation rejection with unchanged foreign user verification, inverse foreign-RH listing exclusion, and non-loopback `BASE_URL` guard unless `ALLOW_RH_USER_MUTATION_EXTERNAL=1`. No sensitive modules are activated. |
| WB-16 | No runtime RED: matrix rows for Admin/RH aggregate dashboard and Lideranca department dashboard were `OK_CURRENT` but lacked real login/JWT E2E proof for company-scope and persisted-department routing. First run failed on test copy expecting unaccented `sessao`; runtime returned the correct accented message. Independent review then flagged missing dashboard-contributing scoped fixtures, weak protected metric schema assertions, incomplete canaries, unsafe external override while seeding local DB, and a loose `127.*` hostname guard. | `cd tests; npx playwright test --config=playwright.config.ts --project=dashboard-scope` passed: 6 tests / 1 project after adding scoped dashboard fixtures, protected schema allowlists, full foreign/dept canaries, hard non-loopback block, and strict loopback hostname regression coverage. `npx tsc --noEmit` and `git diff --check` passed. Claude CLI was installed but blocked by `Not logged in`; independent subagent re-review found no remaining blocking findings. | Added `dashboard-scope` Playwright project with real Admin/RH/Lideranca login, two-company setup, direct Playwright SQLite dashboard fixtures (10 dept A, 11 dept B, 7 foreign), Admin Master required company scope, Admin explicit company protected exam count `21`, RH foreign company override denial, Lideranca persisted department protected exam count `10` despite requested dept B, no-department Lideranca denial, protected headers, exact top-level/nested projection allowlists, protected metric schema checks across metrics/series, foreign/dept/contributor canary exclusions, and hard non-loopback `BASE_URL` block with strict hostname allowlist for `localhost`, `[::1]`, and exact IPv4 `127.0.0.0/8` addresses. No sensitive modules are activated. |
| WB-17 | First run of `cd tests; npx playwright test --config=playwright.config.ts --project=collaborator-journey` failed because the spec assumed `/api/collaborator/challenges` still returned `410 privacy_review`; runtime correctly exposed the rebuilt safe challenge catalog. Independent review then flagged false-pass gaps for foreign exams, external-loopback mutation cleanup, and non-empty challenge participation/projection. | `cd tests; npx playwright test --config=playwright.config.ts --project=collaborator-journey` passed: 6 tests / 1 project after asserting exact personal agenda/exam results, own/foreign campaign and challenge canaries, strict public challenge projection, loopback plus same-DB attestation for external `BASE_URL`, and WB-17 teardown cleanup. `npx tsc --noEmit` and `git diff --check` passed. Claude CLI was installed but blocked by `Not logged in`; independent subagent re-review found no remaining blocking findings. | Added `collaborator-journey` Playwright project with real Admin/RH/Colaboradora login, two-company setup, home summary without legacy gamification fields, campaign join without points/rewards, personal agenda/exam isolation, check-in/streak private shape, leaderboard privacy-review containment, safe company challenge participation, public challenge projection without `user_id`/`company_id`, and teardown cleanup for users, campaigns, exams, challenges and participation ledger data. No sensitive modules are activated. |
| WB-18 | No runtime RED: matrix row for dashboard CSV/export was `OK_CURRENT`, but lacked an explicit regression canary for UI-only/internal view-model fields in the export fixture. | `npm run test:unit -- tests/unit/platform/dashboard-export.test.ts tests/unit/privacy/report-projection.test.ts` passed: 2 files / 27 tests after adding action-description, department-id/color and age-color canaries to the dashboard export fixture. `npx tsc --noEmit` and `git diff --check` passed. Claude CLI was installed but blocked by `Not logged in`; independent subagent review found no blocking findings. | Strengthened the dashboard CSV unit gate so export continues to include only intended protected public cells, formula-neutralized text, visible aggregate values, and suppression messages; it does not serialize action descriptions, department ids/colors, age colors, raw contributor counts, privacy internals, or numeric canaries. No product/runtime export code changed. |
| WB-19 | Initial browser gate exposed test fragility around accent-sensitive headings, then confirmed a real boundary nuance: the visual fixture may leave the demo company with `nr1` enabled, so `/avaliacao-nr1` can render the direct route. In production build it still shows `Runtime Yavix indisponivel`, no COPSOQ form, and the preview disclaimer says no laudo, scoring, GRO/PGR or real Yavix integration. Independent read-only audit also flagged the common shell copy `Desenho aprovado para a proxima etapa` as product-overclaim risk. | `cd tests; npx playwright test --config=playwright.config.ts --project=platform-product-boundary` passed: 14 tests / 1 project after adding the product-boundary smoke, stabilizing text canaries, and addressing independent review warning WR-01 by normalizing visible button/link text before unsafe CTA matching. `npm run test:unit -- tests/unit/module-shells.test.ts` passed: 1 file / 12 tests. `npx tsc --noEmit` and `git diff --check` passed. Claude CLI remains blocked by `Not logged in`; independent subagent review found no blockers. | Added `platform-product-boundary` Playwright project with loopback-only seeded-account guard, authenticated coverage for `/produtos-modulos`, `/concierge`, `/canal-denuncias`, `/viva-sipat`, `/desenvolvimento-humano`, `/nr1`, `/gamificacao-config`, `/liga/gerenciar`, `/liga`, `/desafios`, `/conquistas`, `/objetivos`, and direct `/avaliacao-nr1`. The common contained shell eyebrow now says `Desenho preparado para avaliacao`, not approval. Remaining follow-up: Admin legacy `BadgesTab`/alert notification types still need a dedicated product-boundary decision or removal canary. |
| WB-20 | `npm run test:unit -- tests/unit/privacy/gamification-safe-projection.test.ts` failed on the new Admin legacy canary because `src/app/(platform)/admin/page.tsx` still contained dead `BadgesTab`, `/api/admin/badges`, badge creation copy, `Pontos`, and Admin alert options for `challenge`/`gamification`. | `npm run test:unit -- tests/unit/privacy/gamification-safe-projection.test.ts` passed: 1 file / 19 tests. `npm run test:unit -- tests/unit/privacy/gamification-api-containment.test.ts` passed: 1 file / 20 tests. `npx tsc --noEmit` and `git diff --check` passed. Independent subagent review found no blocker, then WR-01 was addressed by adding behavioral rejection before DB access. | Removed dead Admin badge CRUD UI and badge helper symbols; narrowed Admin alert UI and send schema to `alert`, `system`, `campaign`, `lesson`; moved `initDb()` after payload validation so legacy alert types return 400 before DB/read/write queue. Existing `/api/admin/badges` containment remains 410. Claude CLI remains blocked by `Not logged in`; independent subagent review substituted it. |
| WB-21 | Source scan found active first-access tour copy still saying `conteudo diario e gamificacao`, which contradicted the decision sheet HOLD for classic gamification. A later attempt to expand the gate into public/sales landing copy was cancelled after the user froze the current online landing. | `npm run test:unit -- tests/unit/privacy/gamification-product-copy-boundary.test.ts` passed: 1 file / 2 tests after limiting the canary to first-access and HOLD/review routes. Landing/public paths show no content diff after quarantine. | First-access now says `conteudo diario e recorrencia guiada`; review routes remain HOLD/review and cannot promise rewards, redemption, leaderboard or points. Public/online landing remains unchanged and out of scope. Product implementation remains HOLD pending Option A/B/C decision in the decision sheet. |
| WB-22 | Independent review of WB-21 found a P3 durability gap: the landing freeze was documented and manually checked, but not executable. | `npm run test:unit -- tests/unit/privacy/landing-freeze-boundary.test.ts` passed: 1 file / 1 test. | Added an executable gate that fails if frozen public landing paths differ from baseline `0fb1d517d9ce91cdab1db3703f33f2611d31abfa` or appear in unstaged/staged git diff during Wave B. This is a Wave B guardrail only; it does not edit or validate the deployed landing content. |
| WB-23 | Current-state audit found stale documentation: the full gap map still described reset-password delivery and imported-identity DSAR as pending P1 fixes even though WB-03 and WB-07 had local receipts and tests. | `npm run test:unit -- tests/unit/privacy/recovery-doc-consistency.test.ts tests/unit/privacy/password-reset-delivery.test.ts` passed: 2 files / 5 tests. | Updated the full gap map and parity matrix to say local P1/P2 repairs are covered by WB-03/WB-06/WB-07/WB-08/WB-09/WB-11, production still needs host gates, and WB-03 must be preserved rather than reopened as an unsafe temporary-password task. |
| WB-24 | Goal objective includes a hard gate against deploy without evidence. The release preflight already existed, but current recovery artifacts lacked an executable consistency check tying HOLD language to that script. | `npm run test:unit -- tests/unit/privacy/recovery-production-hold-boundary.test.ts` passed: 1 file / 1 test. `node --test tests/check-release-env-security.test.cjs` passed: 4 tests. | Added a canary that requires current recovery docs to keep production HOLD until target-host evidence exists and confirms `scripts/check-release-env.cjs` enforces release-like prerequisites: JWT secrets, employee-import HMAC secret, persistent access-token blacklist and configured smoke accounts. No deploy was run. |
| WB-25 | Sensitive module HOLD existed in code, but executable evidence was narrow: the Admin module API test only exercised `nr1`, not the full sensitive-module set. | `npm run test:unit -- tests/unit/company-modules.test.ts tests/unit/company-modules-api.test.ts tests/unit/module-shells.test.ts tests/unit/privacy/semaforo-containment.test.ts tests/unit/privacy/gamification-api-containment.test.ts` passed: 5 files / 54 tests. | Strengthened the library/API canaries so Admin module mutation cannot set `primary_health`, `concierge`, `nr1`, `sipat`, `human_development` or `denunciation` to `enabled` without a separate contract/source path. Shell, Semaforo and legacy gamification containment gates stayed green. No module activation, deploy, push or landing change was performed. |
